services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: music_explorer
      POSTGRES_PASSWORD: music_explorer
      POSTGRES_DB: music_explorer
    ports:
      - "5432:5432"
    volumes:
      - postgres-data:/var/lib/postgresql/data

volumes:
  postgres-data:
